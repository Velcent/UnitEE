# Bridge surface

<!-- GENERATED by tools/bindgen/bindgen.py from runtime/src/bridge/bridge-api.json. Do not edit by hand; run 'python tools/bindgen/bindgen.py gen-all' (ADR-002). -->

The complete managed<->native boundary (ADR-002). 94 functions, 4 shared structs.

## Structs

### `P2Vec3` (12 bytes) = `UnityEngine.Vector3`

| field | type | offset |
|---|---|---|
| `x` | `f32` | 0 |
| `y` | `f32` | 4 |
| `z` | `f32` | 8 |

### `P2Quat` (16 bytes) = `UnityEngine.Quaternion`

| field | type | offset |
|---|---|---|
| `x` | `f32` | 0 |
| `y` | `f32` | 4 |
| `z` | `f32` | 8 |
| `w` | `f32` | 12 |

### `P2RaycastHit` (40 bytes) = `UnityEngine.Internal.NativeRaycastHit`

| field | type | offset |
|---|---|---|
| `pointX` | `f32` | 0 |
| `pointY` | `f32` | 4 |
| `pointZ` | `f32` | 8 |
| `normalX` | `f32` | 12 |
| `normalY` | `f32` | 16 |
| `normalZ` | `f32` | 20 |
| `distance` | `f32` | 24 |
| `collider` | `i32` | 28 |
| `triangle` | `i32` | 32 |
| `layer` | `i32` | 36 |

### `P2Contact` (44 bytes) = `UnityEngine.Internal.NativeContact`

| field | type | offset |
|---|---|---|
| `pointX` | `f32` | 0 |
| `pointY` | `f32` | 4 |
| `pointZ` | `f32` | 8 |
| `normalX` | `f32` | 12 |
| `normalY` | `f32` | 16 |
| `normalZ` | `f32` | 20 |
| `colliderA` | `i32` | 24 |
| `colliderB` | `i32` | 28 |
| `phase` | `i32` | 32 |
| `isTrigger` | `i32` | 36 |
| `separation` | `f32` | 40 |

## Functions

| symbol | signature | notes |
|---|---|---|
| `ps2ur_debug_log` | `void(cstr message)` | UTF-8 message to the EE console at Info level. Diagnostic path; the per-call string copy is accepted (ADR-002 cstr rules). |
| `ps2ur_debug_overlay_set_page` | `void(i32 page)` | Selects the on-screen diagnostics page: 0 off, 1 frame timing, 2 profiler zones, 3 memory regions, 4 VRAM allocations. Select on the pad cycles the same pages; values out of range turn it off. |
| `ps2ur_debug_overlay_page` | `i32()` | The diagnostics page currently shown (see ps2ur_debug_overlay_set_page). |
| `ps2ur_light_set` | `void(i32 handle, i32 kind, f32 r, f32 g, f32 b, f32 range, f32 spotCos, i32 enabled)` | Creates or updates the Light on an entity (M14): kind 0 directional, 1 point, 2 spot; colour premultiplied by intensity; range and cos(half spot angle) for the two local kinds. The light's direction and position come from the entity's transform each frame. |
| `ps2ur_scene_set_ambient` | `void(f32 r, f32 g, f32 b)` | RenderSettings.ambientLight (M14): the ambient term the lit programs add, 0..1 per channel. |
| `ps2ur_entity_create` | `i32(i32 parentHandle)` | Creates a root entity (parentHandle 0) or a child. Returns the new handle, or 0 if the entity table is full. |
| `ps2ur_entity_destroy` | `void(i32 handle)` | Destroys the entity and its children. The handle's generation is retired: stale handles fail alive() forever after. |
| `ps2ur_entity_alive` | `i32(i32 handle)` | 1 if the handle refers to a live entity, else 0. This is the native truth behind Unity's destroyed-compares-null semantics. |
| `ps2ur_entity_set_active` | `void(i32 handle, i32 active)` | GameObject.SetActive: the renderer skips inactive subtrees. |
| `ps2ur_entity_get_active` | `i32(i32 handle)` | activeSelf (not the inherited activeInHierarchy). |
| `ps2ur_tf_get_local_position` | `void(i32 handle, P2Vec3*/out value)` |  |
| `ps2ur_tf_set_local_position` | `void(i32 handle, f32 x, f32 y, f32 z)` |  |
| `ps2ur_tf_get_local_rotation` | `void(i32 handle, P2Quat*/out value)` |  |
| `ps2ur_tf_set_local_rotation` | `void(i32 handle, f32 x, f32 y, f32 z, f32 w)` |  |
| `ps2ur_tf_get_local_scale` | `void(i32 handle, P2Vec3*/out value)` |  |
| `ps2ur_tf_set_local_scale` | `void(i32 handle, f32 x, f32 y, f32 z)` |  |
| `ps2ur_tf_get_parent` | `i32(i32 handle)` | Parent handle, or 0 for a root entity. |
| `ps2ur_tf_set_parent` | `void(i32 handle, i32 parentHandle)` | Reparents (parentHandle 0 = make root). Local TRS is NOT adjusted here; Transform.SetParent(worldPositionStays) does that math on the managed side where Unity's semantics live. |
| `ps2ur_tf_child_count` | `i32(i32 handle)` |  |
| `ps2ur_tf_get_child` | `i32(i32 handle, i32 index)` | Child handle by index, or 0 if out of range. |
| `ps2ur_anim_update` | `void(f32 dt)` | Advances every animator by dt and writes their root motion back to entity transforms. The managed dispatcher calls this AFTER Update and coroutines and BEFORE LateUpdate, which is the ordering Unity users rely on (M9 task 4). |
| `ps2ur_animator_play` | `i32(i32 handle, u32 stateHash)` | Jumps the entity's animator to the named baked state with no blend. Returns 1 when the state exists, 0 otherwise. Names are FNV-1a-64 hashes truncated to 32 bits, matching the exporter. |
| `ps2ur_animator_crossfade` | `i32(i32 handle, u32 stateHash, f32 seconds)` | Starts a crossfade to the named state over a duration in SECONDS. Returns 1 when the state exists. |
| `ps2ur_animator_set_trigger` | `void(i32 handle, u32 paramHash)` |  |
| `ps2ur_animator_set_float` | `void(i32 handle, u32 paramHash, f32 value)` |  |
| `ps2ur_animator_is_blending` | `i32(i32 handle)` | 1 while a crossfade is running on the entity's animator. |
| `ps2ur_input_update` | `void()` | Polls every pad. The managed dispatcher calls this once at the top of the frame, before any script runs. |
| `ps2ur_input_button` | `i32(i32 port, i32 button)` | 1 while the button is held. 'button' is the ps2ur::input::Button ordinal. |
| `ps2ur_input_button_down` | `i32(i32 port, i32 button)` |  |
| `ps2ur_input_button_up` | `i32(i32 port, i32 button)` |  |
| `ps2ur_input_axis` | `f32(i32 port, i32 rightStick, i32 vertical)` | Stick axis in -1..1 with the deadzone applied; Y is positive up. |
| `ps2ur_input_pressure` | `i32(i32 port, i32 button)` | Analog button pressure, 0..255, or 0 when the pad does not report it. |
| `ps2ur_input_connected` | `i32(i32 port)` |  |
| `ps2ur_input_set_rumble` | `void(i32 port, i32 smallMotor, i32 largeMotor)` | Small motor is on/off; large motor takes 0..255. |
| `ps2ur_scene_load_begin` | `i32(cstr path, i32 additive)` | Starts an async scene load into the host-provided scene buffer (bridge::bind_scene_buffer). additive != 0 appends to the running world instead of replacing it. Returns 1 when the load started. Only one load is in flight at a time; starting a second while one runs is refused. |
| `ps2ur_scene_load_update` | `i32(i32 byteBudget)` | Advances the load by at most byteBudget bytes and returns the LoadState ordinal (0 Idle, 1 Reading, 2 Parsing, 3 Ready, 4 Failed). A budget of 0 means one chunk. |
| `ps2ur_scene_load_progress` | `f32()` | 0..1 across read and parse together -- what AsyncOperation.progress returns. |
| `ps2ur_scene_load_state` | `i32()` | The LoadState ordinal without advancing the load. |
| `ps2ur_scene_load_set_allow_activation` | `void(i32 allow)` | AsyncOperation.allowSceneActivation. 0 parks a completed read at progress 0.9 without swapping the world; setting it back to 1 lets the next update finish. Defaults to 1 on every begin. |
| `ps2ur_phys_step` | `void()` | Advances the physics world by one fixed step against the scene's own transforms. The managed dispatcher calls this from its FixedUpdate loop, before FixedUpdate scripts run. |
| `ps2ur_phys_set_gravity` | `void(f32 x, f32 y, f32 z)` | Physics.gravity. |
| `ps2ur_phys_fixed_timestep` | `f32()` | Time.fixedDeltaTime as the solver sees it. |
| `ps2ur_phys_set_fixed_timestep` | `void(f32 dt)` |  |
| `ps2ur_phys_set_layers_collide` | `void(i32 a, i32 b, i32 collide)` | One cell of Unity's layer collision matrix. Symmetric: setting (a,b) also sets (b,a). |
| `ps2ur_phys_raycast` | `i32(f32 originX, f32 originY, f32 originZ, f32 dirX, f32 dirY, f32 dirZ, f32 maxDistance, i32 mask, P2RaycastHit* hit)` | Physics.Raycast. Returns 1 on a hit and fills the caller-owned P2RaycastHit; 0 leaves it untouched. |
| `ps2ur_phys_spherecast` | `i32(f32 originX, f32 originY, f32 originZ, f32 radius, f32 dirX, f32 dirY, f32 dirZ, f32 maxDistance, i32 mask, P2RaycastHit* hit)` | Physics.SphereCast, same contract as the raycast above. |
| `ps2ur_phys_overlap_sphere` | `i32(f32 x, f32 y, f32 z, f32 radius, i32 mask)` | Physics.OverlapSphere. Runs the query, keeps the result in a native scratch list and returns how many were FOUND -- which may exceed what the list can hold, so a caller can tell it was truncated. Read the entries with ps2ur_phys_overlap_result. |
| `ps2ur_phys_overlap_result` | `i32(i32 index)` | Collider index from the last overlap query, or -1 when the index is past what the scratch list actually holds. |
| `ps2ur_phys_contact_count` | `i32()` | Contacts produced by the last step. The managed dispatcher reads the whole list in ONE interop call per frame (M11 task 3) rather than one call per event. |
| `ps2ur_phys_get_contact` | `i32(i32 index, P2Contact* contact)` | Fills a caller-owned P2Contact from the last step's list. Returns 0 if the index is out of range. |
| `ps2ur_phys_contacts_dropped` | `i32()` | Contacts that did not fit this frame. Non-zero means the game is silently missing events, and the runtime says so rather than hiding it. |
| `ps2ur_phys_add_body` | `i32(i32 colliderIndex, f32 mass, i32 useGravity, i32 isKinematic)` | Creates a Rigidbody driving the given collider. Returns the body index, or -1 when the table is full. |
| `ps2ur_phys_body_set_velocity` | `void(i32 body, f32 x, f32 y, f32 z)` |  |
| `ps2ur_phys_body_get_velocity` | `void(i32 body, P2Vec3* value)` |  |
| `ps2ur_phys_body_add_force` | `void(i32 body, f32 x, f32 y, f32 z)` | Rigidbody.AddForce in Force mode; the accumulator is cleared each step. |
| `ps2ur_phys_body_set_damping` | `void(i32 body, f32 linearDamping, f32 angularDamping)` | Rigidbody.linearDamping / angularDamping. Separate from add_body because they are settable at any time; a body created without them would silently ignore what the scene exported. |
| `ps2ur_phys_body_set_flags` | `void(i32 body, i32 useGravity, i32 isKinematic, i32 freezeRotation)` | Rigidbody.useGravity / isKinematic / freezeRotation. add_body takes the first two as well, but only as initial values; without this setter changing either after binding would be a property that reads back what you set and changes nothing. |
| `ps2ur_phys_collider_count` | `i32()` | Colliders in the table, including the ones the PHYS section baked. Walked once at boot to map collider indices to GameObjects for the contact callbacks. |
| `ps2ur_phys_collider_entity` | `i32(i32 index)` | The entity HANDLE a collider rides on, or 0 for baked static geometry that belongs to no entity. |
| `ps2ur_phys_collider_for_entity` | `i32(i32 handle)` | The collider index riding on an entity, or -1. Baked colliders arrive from the PHYS section already bound to their entity. |
| `ps2ur_phys_add_character` | `i32(i32 handle, f32 radius, f32 height, f32 slopeLimit, f32 stepOffset, f32 centerX, f32 centerY, f32 centerZ)` | Creates a CharacterController on an entity. Dimensions and center are WORLD units (the managed shim applies Unity's transform scaling). Returns its index, or -1. |
| `ps2ur_phys_move_character` | `i32(i32 index, i32 handle, f32 x, f32 y, f32 z)` | CharacterController.Move. Writes the position reached back to the entity transform and returns Unity's CollisionFlags bits. |
| `ps2ur_phys_character_grounded` | `i32(i32 index)` | CharacterController.isGrounded. |
| `ps2ur_audio_play` | `i32(i32 entity_handle, i32 clip, f32 volume, i32 priority, i32 spatial, f32 min_distance, f32 max_distance)` | Starts a clip on the mixer and returns its voice handle, or 0 when every voice is busy with something more important -- a dropped sound is normal, never an error. Looping is baked into the clip at encode. When 'spatial' is nonzero the voice registers for per-frame pan/attenuation against the entity named by 'entity_handle' (M12.5 task 2). |
| `ps2ur_audio_stop` | `void(i32 voice)` | Stops a voice. 0 and stale handles are no-ops. |
| `ps2ur_audio_is_playing` | `i32(i32 voice)` | 1 while the voice is sounding. AudioSource.isPlaying reads this. |
| `ps2ur_audio_set_volume` | `void(i32 voice, f32 volume)` | Live volume change on a sounding voice. A spatial voice keeps its per-frame pan; a 2D voice sits centred. |
| `ps2ur_audio_clip_count` | `i32()` | Clips loaded from the SND section. |
| `ps2ur_audio_clip_seconds` | `f32(i32 clip)` | A clip's duration in seconds, from its sample count and pitch. AudioClip.length reads this. |
| `ps2ur_prefs_set_int` | `void(cstr key, i32 value)` | PlayerPrefs.SetInt into the M10 memcard store (in memory until ps2ur_prefs_save). Keys cap at 24 bytes, the store at 64 entries (deviation 28). cstr use is deliberate: cold path, tiny strings, and hashed keys would make GetString impossible. |
| `ps2ur_prefs_set_float` | `void(cstr key, f32 value)` | PlayerPrefs.SetFloat. |
| `ps2ur_prefs_set_string` | `void(cstr key, cstr value)` | PlayerPrefs.SetString. Values cap at 48 bytes (deviation 28). |
| `ps2ur_prefs_get_int` | `i32(cstr key, i32 fallback)` | PlayerPrefs.GetInt with its fallback. |
| `ps2ur_prefs_get_float` | `f32(cstr key, f32 fallback)` | PlayerPrefs.GetFloat with its fallback. |
| `ps2ur_prefs_has_key` | `i32(cstr key)` | PlayerPrefs.HasKey. |
| `ps2ur_prefs_delete_key` | `void(cstr key)` | PlayerPrefs.DeleteKey. |
| `ps2ur_prefs_delete_all` | `void()` | PlayerPrefs.DeleteAll. |
| `ps2ur_prefs_string_length` | `i32(cstr key)` | Byte length of a string value, or -1 when the key is absent or not a string. A returned pointer would need an owner the boundary refuses to have, so GetString reads length + bytes and caches managed-side. |
| `ps2ur_prefs_string_byte` | `i32(cstr key, i32 index)` | Byte at index of a string value, or -1 past the end. Cold path by design: a 48-byte value read once and cached. |
| `ps2ur_prefs_save` | `i32()` | Writes the store to memory card port 0 under the identity the host set at boot. Returns memcard::Status: 0 Ok, then NoCard, NotFormatted, Full, WriteProtected, NotFound, Error. Unity's Save cannot fail; this one reports through PS2Memory.LastSaveError (deviation 29). |
| `ps2ur_particles_play` | `void(i32 entity_handle)` | PS2ParticleSystem.Play (ADR-011): starts emission and fires the authored burst. Addressed by the owning entity's handle. |
| `ps2ur_particles_stop` | `void(i32 entity_handle)` | PS2ParticleSystem.Stop: emission ceases, live particles play out -- Unity's Stop semantics. |
| `ps2ur_particles_emit` | `void(i32 entity_handle, i32 count)` | PS2ParticleSystem.Emit(count). Spawns up to the pool's remaining capacity; the budget is the contract. |
| `ps2ur_particles_is_playing` | `i32(i32 entity_handle)` | 1 while the system is emitting. |
| `ps2ur_particles_count` | `i32(i32 entity_handle)` | Live particles right now; PS2ParticleSystem.particleCount. |
| `ps2ur_ui_element_for_entity` | `i32(i32 entity_handle)` | The uGUI draw-table index for an entity, or -1. Elements are flat-table indexed like colliders; managed UI components hold this for their setters. |
| `ps2ur_ui_set_rect` | `void(i32 element, f32 x, f32 y, f32 w, f32 h)` | Moves/resizes an element in screen pixels, top-left origin. Layout was baked at export; this is how scripts (and the Slider fill) animate it. |
| `ps2ur_ui_get_colour` | `u32(i32 element)` | The element's CURRENT tint, RGBA8 with alpha in the PS2 0..0x80 range. CreateUIGraphic seeds the managed Graphic from this so the focus highlight can restore the AUTHORED colour, not an assumed white. Out-of-range elements return opaque white. |
| `ps2ur_ui_set_colour` | `void(i32 element, u32 rgba)` | Tint, RGBA8 with alpha already in the PS2 0..0x80 range. Graphic.color and the navigation focus highlight write through this. |
| `ps2ur_ui_set_text` | `void(i32 element, cstr text)` | Replaces a text element's string (48-byte cap, baked 8x8 font). cstr on a set-on-change path, not per frame. |
| `ps2ur_ui_set_text_glow` | `void(i32 element, f32 spread, f32 intensity, f32 dilate)` | Console-side bloom for a text element: the glyph run is drawn again in a ring of 'spread' pixels, additively at 'intensity' (0..1) of its alpha, under the crisp run; 'dilate' (0..1) widens the inner ring. 0 intensity is plain text. PS2BootGlowText's UNITY_PS2 branch drives it. |
| `ps2ur_ui_set_visible` | `void(i32 element, i32 visible)` | Shows/hides an element; Behaviour.enabled on the managed graphics. |
| `ps2ur_ui_set_align` | `void(i32 element, i32 align_h, i32 align_v)` | Text.alignment / TMP_Text.alignment: 0 left/top, 1 centre/middle, 2 right/bottom. Applied per line at draw time. |
| `ps2ur_ui_get_align` | `u32(i32 element)` | The authored alignment of an element: align_h | (align_v << 8), so a graphic's managed view starts from what the exporter baked. |
